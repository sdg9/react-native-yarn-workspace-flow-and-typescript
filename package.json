{ 
    "name": "workspace-root",
    "private": true,
    "workspaces": {
        "packages": ["app-mobile", "packages/*"]
    },
    "scripts": {
        "android": "yarn workspace app-mobile android",
        "start": "yarn workspace app-mobile start",
        "flow": "yarn workspace app-mobile flow",
        "test": "yarn workspace app-mobile test",
        "lint": "yarn workspace app-mobile lint"
    }
}